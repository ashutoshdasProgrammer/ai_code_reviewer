## I have created a AI_code_review.

# The project has been created with the help of NodeJs (backend) and ReactJs (Frontend).

Here, you get a code editor where you can write your code manually or copy/paste your code.

In the project create a .env file in the backend folder structure.

There you can add GROQ_API_KEY / OPENAI_API_KEY / GEMINI_API_KEY.

install the dependencies in the package.json through npm.

run the backend through "nodemon server.js"and frontend through "npm run dev".

""" The project takes your code as input and then makes api calls so that the critical vulnerabilities in your code can be found and fixed with the help of AI. I intend to use to project further and create a VSCODE extension for finding criticality in backend as there is "react doctor" for frontend but nothing for backend. This codebase is the first step towards the project. """

Visit my repository VulnerabilityFinder for creating VSExtension for this project.